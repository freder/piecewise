{
  "name": "@freder/piecewise",
  "version": "0.3.1",
  "main": "dist/index.js",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-jest": "^19.0.0",
    "babel-preset-es2015": "^6.22.0",
    "canvas": "^1.6.3",
    "eslint-config-airbnb-base": "^11.1.0",
    "jest": "^19.0.2"
  },
  "dependencies": {
    "ramda": "^0.23.0"
  },
  "scripts": {
    "test": "jest",
    "test-watch": "yarn run test -- --watch --notify",
    "compile": "babel --presets es2015 -d dist/ src/",
    "prepublish": "npm run compile"
  }
}
